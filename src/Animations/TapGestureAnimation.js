import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

const TapGestureAnimation = () => {
  return (
    <View>
      <Text>TapGestureAnimation</Text>
    </View>
  )
}

export default TapGestureAnimation

const styles = StyleSheet.create({})